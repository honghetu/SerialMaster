<UserControl x:Class="SerialMaster.UI.Views.FileTransferView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding ModeText}" FontSize="14" FontWeight="SemiBold"
                   Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,12"/>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}"
                     IsReadOnly="True" FontSize="12" Padding="8,6"
                     Background="{DynamicResource SecondaryBackgroundBrush}"
                     Foreground="{DynamicResource PrimaryTextBrush}"
                     BorderBrush="{DynamicResource SurfaceBorderBrush}"
                     VerticalContentAlignment="Center"/>
            <Button Grid.Column="1" Content="选择文件..." Width="90" Margin="8,0,0,0"
                    Command="{Binding BrowseFileCommand}"
                    Background="{DynamicResource SurfaceBrush}"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    BorderBrush="{DynamicResource SurfaceBorderBrush}"/>
            <Button Grid.Column="2" Content="发送 (XMODEM)" Width="110" Margin="8,0,0,0"
                    Command="{Binding SendFileCommand}"
                    Background="{DynamicResource AccentBrush}"
                    Foreground="{DynamicResource PrimaryBackgroundBrush}" BorderThickness="0"/>
        </Grid>

        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource SurfaceBorderBrush}" BorderThickness="1"
                CornerRadius="4" Padding="10" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="或接收: " VerticalAlignment="Center" FontSize="12"
                           Foreground="{DynamicResource SecondaryTextBrush}"/>
                <Button Content="开始接收 (XMODEM-CRC)" Margin="6,0,0,0" Padding="10,4"
                        Command="{Binding ReceiveFileCommand}"
                        Background="{DynamicResource SuccessBrush}"
                        Foreground="#1E1E2E" BorderThickness="0"/>
                <TextBlock Text="发送方需在 30 秒内开始 XMODEM 发送" Margin="14,0,0,0"
                           VerticalAlignment="Center" FontSize="11"
                           Foreground="{DynamicResource MutedTextBrush}"/>
            </StackPanel>
        </Border>

        <!-- Progress -->
        <Grid Grid.Row="3" Margin="0,0,0,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ProgressBar Grid.Row="0" Height="20" Minimum="0"
                         Maximum="{Binding ProgressMax}" Value="{Binding Progress}"
                         Foreground="{DynamicResource SuccessBrush}"
                         Background="{DynamicResource SecondaryBackgroundBrush}"
                         BorderBrush="{DynamicResource SurfaceBorderBrush}"/>
            <Grid Grid.Row="1" Margin="0,4,0,0">
                <TextBlock Text="{Binding StatusText}" FontSize="12"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           HorizontalAlignment="Left"/>
                <Button Content="取消" Width="60"
                        IsEnabled="{Binding IsTransferring}"
                        Command="{Binding CancelTransferCommand}"
                        Background="{DynamicResource ErrorBrush}"
                        Foreground="{DynamicResource PrimaryBackgroundBrush}"
                        BorderThickness="0" HorizontalAlignment="Right"/>
            </Grid>
        </Grid>

        <!-- Log -->
        <Border Grid.Row="4" Background="{DynamicResource SecondaryBackgroundBrush}"
                BorderBrush="{DynamicResource SurfaceBorderBrush}"
                BorderThickness="1" CornerRadius="4" Padding="8">
            <TextBox Text="{Binding TransferLog, Mode=OneWay}" IsReadOnly="True"
                     FontFamily="Consolas" FontSize="11"
                     Background="Transparent"
                     Foreground="{DynamicResource SecondaryTextBrush}"
                     BorderThickness="0"
                     VerticalScrollBarVisibility="Auto"
                     TextWrapping="Wrap"/>
        </Border>
    </Grid>
</UserControl>
