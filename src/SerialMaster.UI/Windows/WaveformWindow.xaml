<Window x:Class="SerialMaster.UI.Windows.WaveformWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctrl="clr-namespace:SerialMaster.UI.Controls"
        Title="波形视图" Height="400" Width="700"
        MinHeight="250" MinWidth="400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource PrimaryBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource SurfaceBorderBrush}"
                BorderThickness="0,0,0,1" Padding="8,4">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="〰 波形视图" FontSize="12" FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="通道数:" FontSize="11"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           VerticalAlignment="Center" Margin="0,0,4,0"/>
                <TextBox x:Name="ChannelCountBox" Text="1" Width="36" FontSize="11"
                         FontFamily="Consolas"
                         Background="{DynamicResource SecondaryBackgroundBrush}"
                         Foreground="{DynamicResource PrimaryTextBrush}"
                         BorderBrush="{DynamicResource SurfaceBorderBrush}"
                         VerticalContentAlignment="Center"
                         TextChanged="OnChannelCountChanged"/>
            </StackPanel>
        </Border>

        <ctrl:WaveformControl Grid.Row="1" x:Name="WaveformCanvas"
                              ChannelCount="1" MaxPoints="500"/>
    </Grid>
</Window>
